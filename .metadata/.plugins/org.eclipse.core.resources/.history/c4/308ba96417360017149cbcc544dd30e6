package test.main;

import test.auto.Car;
public class MainClass01 {
	//일반 멤버필드 정의하기
	public String brand="아우디"; //클래스 안에서 정의하는건 변수가아니라 필드
	
	//일반멤버 메소드 정의하기
	public static void main(String[] args) { //메소드안에서 정의해야 지역변수
		new Car().drive();
	}
}
